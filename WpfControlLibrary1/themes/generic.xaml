<ResourceDictionary
	 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	 xmlns:local="clr-namespace:BetterExplorerControls"
		>

  <Style TargetType="{x:Type local:ListviewColumnDropDown}" BasedOn="{StaticResource {x:Type ContextMenu}}">
    <Setter Property="SnapsToDevicePixels"
          Value="True" />
    <Setter Property="Grid.IsSharedSizeScope"
          Value="true" />
    <Setter Property="HasDropShadow"
          Value="False" />
    <Setter Property="ItemContainerStyle">
      <Setter.Value>
        <Style TargetType="{x:Type MenuItem}">
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="{x:Type MenuItem}">
                <Border BorderBrush="Black" BorderThickness="0">
                  <StackPanel Orientation="Horizontal">
                    <CheckBox Margin="7,2,2,2" VerticalAlignment="Center" IsChecked="{TemplateBinding IsChecked}"/>
                    <ContentPresenter x:Name="content"  ContentSource="Header" Margin="15,2,2,2" HorizontalAlignment="Stretch"/>
                  </StackPanel>
                </Border>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </Setter.Value>
    </Setter>
    
  </Style>


</ResourceDictionary>
